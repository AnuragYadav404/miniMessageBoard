extends layout

block content
  h1 Message Board
  .Messages   
    ul
      each message in messageArray
        li 
          .message 
            p 
              b #{message.user} says: #{message.text} at: #{message.added}
            
  a(href="/new") Send Message